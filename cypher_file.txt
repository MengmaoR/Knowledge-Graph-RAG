
            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(n) WHERE n[prop] = '国家：美国')
            RETURN apoc.map.fromPairs([key in keys(m)[..5] | [key, m[key]]]) AS m_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(m) WHERE m[prop] = '国家：美国')
            RETURN apoc.map.fromPairs([key in keys(n)[..5] | [key, n[key]]]) AS n_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(n) WHERE n[prop] = '美利坚合众国')
            RETURN apoc.map.fromPairs([key in keys(m)[..5] | [key, m[key]]]) AS m_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(m) WHERE m[prop] = '美利坚合众国')
            RETURN apoc.map.fromPairs([key in keys(n)[..5] | [key, n[key]]]) AS n_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(n) WHERE n[prop] = 'United States')
            RETURN apoc.map.fromPairs([key in keys(m)[..5] | [key, m[key]]]) AS m_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(m) WHERE m[prop] = 'United States')
            RETURN apoc.map.fromPairs([key in keys(n)[..5] | [key, n[key]]]) AS n_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(n) WHERE n[prop] = 'United States of America')
            RETURN apoc.map.fromPairs([key in keys(m)[..5] | [key, m[key]]]) AS m_limited_properties, 
                   type(r) AS relationship_type
            

            MATCH (n)-[r:IN_COUNTRY|ON_CONTINENT]->(m)
            WHERE any(prop in keys(m) WHERE m[prop] = 'United States of America')
            RETURN apoc.map.fromPairs([key in keys(n)[..5] | [key, n[key]]]) AS n_limited_properties, 
                   type(r) AS relationship_type
            
